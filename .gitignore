__pycache__/
*.pyc
.venv/

# Fetched/derived state, not source. draft_state.json and elo_overrides.json are
# deliberately NOT here: the draft is over, so the roster and picks are league
# data the deployed season tracker needs to have with it, and elo_overrides.json
# is manually-curated league data in the same sense.
season_results.json
notify_state.json
*.json.tmp

frontend/node_modules/
frontend/dist/
draftboard/node_modules/
draftboard/dist/
